services:

  portfolio:
    image: portfolio-app
    container_name: portfolio
    ports:
      - "8081:80"
    restart: always

  java-app:
    image: java-app
    container_name: java-container
    ports:
      - "8080:8080"
    restart: always
