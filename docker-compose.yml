version: "3.8"

services:
  portfolio:
    image: stanphlux007/portfolio-app:latest
    container_name: portfolio
    ports:
      - "80:80"
    restart: always

  java-app:
    image: stanphlux007/java-app:latest
    container_name: java-app
    ports:
      - "8080:8080"
    restart: always
